import Wrapper from "../Wrapper";
import SearchBar from "../SearchBar";
import List from "../List";

const Whapi = () => {
  return (
    <Wrapper>
      <SearchBar />
      <List />
    </Wrapper>
  );
};

export default Whapi;
